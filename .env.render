# Variables de Entorno para Render
# Copia estos valores en la configuración de Environment Variables en Render

# ======================
# DATABASE
# ======================
# Usa la "Internal Database URL" que te proporciona Render PostgreSQL
DATABASE_URL=postgresql://ecommerce_user:PASSWORD@hostname/ecommerce_db

# ======================
# JWT Configuration
# ======================
# IMPORTANTE: Genera una nueva clave secreta de 256+ bits para producción
# Comando para generar: openssl rand -base64 64
JWT_SECRET=GENERA_UNA_CLAVE_SECRETA_AQUI_DE_AL_MENOS_256_BITS
JWT_EXPIRATION=86400

# ======================
# Spring Configuration
# ======================
SPRING_PROFILES_ACTIVE=production

# ======================
# Java Options
# ======================
# Para Free/Starter Plan (512 MB RAM):
JAVA_OPTS=-Xms256m -Xmx400m -XX:+UseContainerSupport -XX:+UseG1GC

# Para Standard Plan (2 GB RAM):
# JAVA_OPTS=-Xms512m -Xmx1536m -XX:+UseContainerSupport -XX:+UseG1GC -XX:MaxGCPauseMillis=150

# Para Professional Plan (4 GB RAM):
# JAVA_OPTS=-Xms1g -Xmx3g -XX:+UseContainerSupport -XX:+UseG1GC -XX:MaxGCPauseMillis=100

# ======================
# File Upload
# ======================
UPLOAD_PATH=/app/uploads/productos

# ======================
# CORS
# ======================
# Reemplaza con la URL de tu frontend en producción
CORS_ALLOWED_ORIGINS=https://tu-frontend.com,https://tu-frontend.vercel.app

# ======================
# Opcional: Cloudinary (para persistir imágenes)
# ======================
# CLOUDINARY_CLOUD_NAME=tu_cloud_name
# CLOUDINARY_API_KEY=tu_api_key
# CLOUDINARY_API_SECRET=tu_api_secret

# ======================
# Opcional: Email (para notificaciones)
# ======================
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=tu_email@gmail.com
# MAIL_PASSWORD=tu_app_password

